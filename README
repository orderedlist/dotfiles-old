My Dot Files

    mkdir ~/bin
    cd ~/bin
    git clone git://github.com/orderedlist/dotfiles.git
    touch ~/.bashrc
    echo ". ~/bin/dotfiles/bashrc" >> ~/.bashrc
    touch ~/.bash_profile
    echo ". ~/.bashrc" >> ~/.bash_profile
    . ~/.bash_profile
